# Build artifacts
bin/
tmp/
release/
build-dir/
coverage.out
*.exe
# Go
.go/
vendor/
# Frontend
frontend/node_modules/
frontend/dist/
internal/server/static/
internal/server/docs_site/
# Data files (user-provided)
data/*.csv
data/*.mbtiles
data/*.parquet
data/*.geoparquet
data/*.json
data/images/
data/sites/
# Large resource files
*.mbtiles
*.gpkg
# IDE
.idea/
.vscode/
*.swp
*.swo
*~
# OS
.DS_Store
Thumbs.db
# Logs
PROMPT.log
PROMPTS.log
# Models
# MkDocs
site/
requirements/site/
# Per-machine launch settings for scripts/run-app.sh (see .dt-env.example)
.dt-env
# Benchmarking (benchmarks/results/ is kept, see Makefile)
benchmarks/dtbench.sqlite
benchmarks/reports/

# Nix
result
.direnv/
.webkit-compat/
diff.txt
dist/
docs/CatchmentCalculations.xlsx
git.sh
claude.sh

# Machine-local Claude Code permissions. Personal to whoever is running it, and
# the tracked copy carried absolute paths under one developer's home directory
# plus references to internal/llm and internal/nn, removed long ago.
.claude/settings.local.json

# One-off browser scripts written against a particular machine's scratch
# directory. Useful while debugging, meaningless to anyone else.
frontend/scratch_test_*.mjs
issue-metadata-mismatch.md
node_modules
docs/assets/diagrams/generated/
docs/hooks/__pycache__/
site/
# Stray data content (see data-readme.md)
data-old/
data/mbtiles/*.json
!data/mbtiles/style.json

# datasources/ — pipeline inputs, never shipped (see datasources-readme.md).
# Config/scripts/small reference files (layer-treatment.csv, style*.json,
# DecisionTheatre.qgz) are tracked; bulk source data is not.
datasources/r-analysis/
datasources/scenarios/*.csv
datasources/catchments/*.gpkg
datasources/catchments/hybas_af_lev01-12_v1c/
datasources/basemap/*.gpkg

# Resumable mbtiles build cache — safe to purge, expensive to regenerate.
.cache/
pr.sh
pr-body.md
pr-body-*.md
issue-*.md

# Environment files are never committed — anywhere, not just where one has
# already caught us out. Build-time configuration belongs to the deployment,
# and a tracked *.env also fails the repository's own secret-scanning gate.
# The .example files are documentation and stay tracked; they do not match
# these patterns.
*.env
.env
.env.*
!.env.example
!*.env.example

# Screenshots shared during development, not part of the source
screenshots-gitignored/
